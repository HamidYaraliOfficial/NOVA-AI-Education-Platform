package com.nova.android.sync

/**
 * Placeholder push-notification receiver. Wire this to Firebase Cloud
 * Messaging (or another push provider) to deliver study reminders, exam
 * alerts and teacher announcements from the Notification Center backend
 * module directly to the device.
 */
class NovaFirebaseMessagingService
